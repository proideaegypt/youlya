# Release: v2.1.1 — playwright-ux-swarm-signal-quality

## Task
fix-playwright-ux-swarm-signal-quality

## Release Type
patch

## What Changed
- Updated dashboard UX swarm network tracking to classify expected Next.js RSC aborted prefetch/navigation failures as ignored framework noise.
- Preserved real-failure enforcement for server/API/network issues by asserting only on non-ignorable failed requests.
- Reworked heading extraction to avoid long timeouts when `h1` is missing and log this as an actionable UX issue instead of a hard test failure.
- Expanded per-route UX markdown/json output with heading presence, body length, shell visibility, build identity, real failures, ignored noise, and issue list.
- Updated swarm report collector to summarize failed tests, ignored noise count, real network failures, missing h1 routes, screenshot index, and P0/P1/P2 UX priority mapping.
- Routed dashboard swarm artifact output to this task path: `qa-artifacts/tasks/2026-05-01/fix-playwright-ux-swarm-signal-quality`.

## Files Changed
- M `tests/playwright/helpers.ts`
- M `tests/playwright/dashboard-ux-swarm.spec.ts`
- M `scripts/collect-playwright-swarm-report.mjs`
- M `package.json`
- M `qa-artifacts/tasks/2026-05-01/fix-playwright-ux-swarm-signal-quality/*` (generated reports/screenshots)

## Tests Required
- npm run test:e2e:dashboard:swarm
- npm run qa:collect
- npm run typecheck
- npm run lint
- npm test
- npm run validate:scenarios
- npm run scan:secrets
- npm run verify:release
- npm run build
- npm run verify:deploy

## Deployment Notes
- Do not ignore real API errors, 500 responses, auth loops, or console crashes.
- Missing `h1` is tracked as P1 UX issue and reported without blocking swarm execution.

## Status
PASS
