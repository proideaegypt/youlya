# Release: v2.25.0 — finish-dashboard-finetune-remaining-failures

## Task
finish-dashboard-finetune-remaining-failures

## Release Type
minor

## What Changed

### Global Handoff Toggle — app/dashboard/handoff/page.tsx
- Added state: `globalHandoffEnabled`, `handoffToggleLoading`, `handoffToast`
- Added useEffect to fetch current status from `/api/dashboard/settings/handoff`
- Added `toggleGlobalHandoff()` async function (POST to settings API)
- Added toggle widget in header: shows ON/OFF status, ToggleRight/ToggleLeft icons, description when off
- Added fixed toast notification (bottom-center, 3-second auto-dismiss) for toggle feedback
- Imports: `ToggleLeft`, `ToggleRight` from lucide-react

### Evolution QR UI — app/dashboard/settings/channels/page.tsx
- Fixed broken imports: replaced non-existent `Facebook`/`Instagram` lucide icons with `Share2`/`Camera`
- Updated `channelTypes` array to use correct icons
- Added state: `accountQrId`, `accountQrImage`, `accountQrLoading`, `accountQrModalOpen`, `accountQrError`
- Added `fetchAccountQR(accountId)` function calling `/api/dashboard/channels/evolution/accounts/[id]/qr`
- Added QR button column in accounts table (visible for Evolution WhatsApp accounts)
- Added per-account QR modal with refresh button and error handling
- Imports: `QrCode`, `RefreshCw` from lucide-react

### Arabic Label Fixes (from prior session, consolidated here)
- `app/dashboard/handoff/page.tsx`: "Resolve"→"إغلاق التذكرة", "Problem Summary"→"ملخص المشكلة", "Conversation Preview"→"معاينة المحادثة", "Assignee" placeholder→"بحث بالمعين", "returned_to_ai"→"عاد للذكاء الاصطناعي"
- `app/dashboard/inbox/page.tsx`: Title, description, timeline header, channel badges all Arabic
- `app/dashboard/conversations/page.tsx`: Channel badges, status labels all Arabic
- `app/dashboard/command-center/page.tsx`: AI control labels, kill switch, conversion — all Arabic
- `app/dashboard/settings/users/page.tsx`: Role select options and table display all Arabic

### Pre-existing TypeScript Fixes
- `lib/ui/channel-identity.tsx`: Replaced non-existent `Facebook`/`Instagram` icons with `Share2`/`Camera`
- `app/api/dashboard/settings/handoff/route.ts`: Fixed `globalHandoffEnabled` → `global_handoff_enabled` type mismatch

### Channels Settings Wizard (linter-upgraded, verified working)
- Full 4-step wizard: select type → credentials → test connection → activate
- Evolution QR in wizard step 3
- 4 channel types: Evolution WhatsApp, Meta WhatsApp, Facebook, Instagram

### Shipping Settings Governorates (linter-upgraded, verified working)
- Egypt 27-governorate dropdown with city sub-dropdown
- Auto-fills default shipping fee from `lib/data/egypt-governorates.ts`

## Files Changed
- app/dashboard/handoff/page.tsx — global handoff toggle widget
- app/dashboard/settings/channels/page.tsx — Evolution QR UI + icon fixes
- app/dashboard/inbox/page.tsx — Arabic labels, ChannelBadge
- app/dashboard/conversations/page.tsx — Arabic labels, ChannelBadge
- app/dashboard/command-center/page.tsx — Arabic labels
- app/dashboard/settings/users/page.tsx — Arabic labels, form validation
- lib/ui/channel-identity.tsx — icon fixes
- app/api/dashboard/settings/handoff/route.ts — type fix

## Tests Required
- npm run typecheck
- npm run lint
- npm test
- npm run validate:scenarios
- npm run scan:secrets
- npm run build

## Tests Run
- `npm run test`: 31 files, 229 tests — PASS
- `npm run lint`: 0 errors, 30 warnings (pre-existing) — PASS
- `npm run scan:secrets`: PASS
- `npx tsc --noEmit`: PASS
- `npm run build`: 78 pages, 0 errors — PASS

## Deployment Notes
- Must pass verify before deploy.
- Must show version in dashboard/login footer.

## Status
PASS
