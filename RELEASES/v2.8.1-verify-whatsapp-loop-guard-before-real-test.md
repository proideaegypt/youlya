# Release: v2.8.1 — verify-whatsapp-loop-guard-before-real-test

## Task
verify-whatsapp-loop-guard-before-real-test

## Release Type
patch

## What Changed
- Verified the live `Youlya WhatsApp Main` workflow is active in n8n.
- Confirmed the production webhook path is `youlya-whatsapp` and the workflow starts from `Webhook` into `Guard Inbound Customer Message`.
- Confirmed `fromMe=true` executions stop at the guard and do not reach `Filter Message Type`, `Normalize Message`, `Call Turn Endpoint`, or `Send Text`.
- Confirmed `fromMe=false` executions pass the guard, reach `Call Turn Endpoint`, and attempt `Send Text` once.
- Checked active workflow inventory for duplicate webhook usage and found no second active workflow using the same `youlya-whatsapp` path.

## Files Changed
- `worktime.md`
- `RELEASES/v2.8.1-verify-whatsapp-loop-guard-before-real-test.md`

## Tests Required
- `n8n_list_workflows` active inventory check
- `n8n_get_workflow` for `joqfame4HXG775JO`
- `n8n_validate_workflow` for `joqfame4HXG775JO`
- Synthetic webhook execution `fromMe=true`
- Synthetic webhook execution `fromMe=false`
- `n8n_executions` filtered execution inspection

## Deployment Notes
- Must pass verify before deploy.
- Must show version in dashboard/login footer.

## Status
PASS
