# Release: v2.6.2 — configure-n8n-runtime-env-for-youlya-whatsapp-workflow

## Task
configure-n8n-runtime-env-for-youlya-whatsapp-workflow

## Release Type
patch

## What Changed
- Configured n8n runtime environment variables for the Youlya WhatsApp workflow.
- Added APP_INTERNAL_URL, INTERNAL_API_SECRET, EVOLUTION_API_URL, EVOLUTION_API_KEY, EVOLUTION_INSTANCE to /root/n8n/docker-compose.yml n8n service.
- Recreated n8n container safely (preserved db/volumes).
- Verified all 5 env vars are present in n8n runtime.
- Verified workflow joqfame4HXG775JO is active and webhook is registered.
- No application code changes; change is infrastructure-only on VPS.

## Files Changed
- /root/n8n/docker-compose.yml (infrastructure, outside git repo)
- /root/n8n/docker-compose.yml.backup.20260502232141 (backup)

## Tests Required
- npm run typecheck
- npm run lint
- npm test
- npm run validate:scenarios
- npm run scan:secrets
- npm run build

## Deployment Notes
- Must pass verify before deploy.
- Must show version in dashboard/login footer.

## Status
PASS
