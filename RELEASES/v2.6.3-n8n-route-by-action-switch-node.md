# Release: v2.6.3 — n8n-route-by-action-switch-node

## Task
fix-n8n-route-by-action-switch-node

## Release Type
patch

## What Changed
- Patched live n8n workflow `joqfame4HXG775JO` via API to replace broken Switch node with Code + IF flow.
- Removed `Route by Action` (Switch node typeVersion 3, incompatible with n8n 2.3.6 — caused `Cannot read properties of undefined (reading 'caseSensitive')`).
- Added `Prepare Reply` (Code node) after `Call Turn Endpoint` to normalize action/reply/shouldSend/number.
- Added `Should Send Reply` (IF node) after `Prepare Reply` to gate sending based on `$json.shouldSend`.
- Updated `Send Text` body to use `{{$json.number}}` and `{{$json.reply}}` from upstream nodes.
- Preserved all env references (`APP_INTERNAL_URL`, `INTERNAL_API_SECRET`, `EVOLUTION_API_URL`, `EVOLUTION_API_KEY`, `EVOLUTION_INSTANCE`).
- Workflow deactivated, updated, reactivated safely. Webhook registration preserved.
- No repo application code changes; change is infrastructure/runtime-only on live n8n.

## Files Changed
- qa-artifacts/tasks/2026-05-02/fix-n8n-route-by-action-switch-node/backup-youlya-whatsapp-main.json
- qa-artifacts/tasks/2026-05-02/fix-n8n-route-by-action-switch-node/RESULT.md

## Tests Required
- npm run typecheck
- npm run lint
- npm test
- npm run validate:scenarios
- npm run scan:secrets
- npm run build

## Deployment Notes
- Must pass verify before deploy.
- Must show version in dashboard/login footer.

## Status
PASS
