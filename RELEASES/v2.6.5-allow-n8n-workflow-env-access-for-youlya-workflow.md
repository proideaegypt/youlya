# Release: v2.6.5 — allow-n8n-workflow-env-access-for-youlya-workflow

## Task
allow-n8n-workflow-env-access-for-youlya-workflow

## Release Type
patch

## What Changed
- Added `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` to the live n8n compose environment in `/root/n8n/docker-compose.yml`.
- Backed up the compose file before editing.
- Restarted only the `n8n` service with `docker compose up -d n8n` from the compose working directory.
- Verified the env flag is present inside the container and set to `false`.
- Ran a synthetic webhook test against the direct n8n port and confirmed the workflow started, the execution record was created, and the flow reached `Call Turn Endpoint` and `Send Text`.
- Confirmed there is no `access to env vars denied` error in the recent n8n logs.
- Observed a separate downstream `Send Text` failure because the `next-link-main` Evolution instance does not exist, and the public Apache front door returned HTTP 500 on the external webhook URL.

## Files Changed
- /root/n8n/docker-compose.yml
- /root/n8n/docker-compose.yml.backup.20260503163751
- RELEASES/v2.6.5-allow-n8n-workflow-env-access-for-youlya-workflow.md
- worktime.md
- PROGRESS-LOG.md

## Tests Required
- npm run typecheck
- npm run lint
- npm test
- npm run validate:scenarios
- npm run scan:secrets
- npm run build

## Deployment Notes
- Must pass verify before deploy.
- Must show version in dashboard/login footer.

## Status
PARTIAL
