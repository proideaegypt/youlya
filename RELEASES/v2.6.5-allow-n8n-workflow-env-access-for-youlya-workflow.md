# Release: v2.6.5 — allow-n8n-workflow-env-access-for-youlya-workflow

## Task
allow-n8n-workflow-env-access-for-youlya-workflow

## Release Type
patch

## What Changed
- Added `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` to the live n8n compose environment in `/root/n8n/docker-compose.yml`.
- Updated the live n8n Evolution settings to use the actual open instance `AI` and the matching live Evolution auth key.
- Backed up the compose file before editing and restarted only the `n8n` service.
- Verified the env flag is present inside the container and set to `false`.
- Verified `EVOLUTION_INSTANCE=AI` inside the container.
- Verified the Evolution API now accepts credentials and lists the `AI` instance as open.
- Ran a synthetic webhook test against the public webhook URL and confirmed it returns HTTP 200, creates an execution, and reaches `Send Text`.
- Confirmed the send path now fails only with a synthetic bad request on the dummy JID payload, not with env access denied, proxy 500, unauthorized, or missing instance errors.
- Fixed the repo lint/typecheck blockers in:
  - `lib/adapters/supabase/product-sync-repository.ts`
  - `lib/services/product-search-service.ts`
  - `lib/services/shopify-product-sync-service.ts`
- Fixed the canonical workflow false-positive by replacing the UUID-style `webhookId` with a non-UUID canonical identifier in `n8n/workflows/youlya-whatsapp-main.json`.

## Files Changed
- /root/n8n/docker-compose.yml
- /root/n8n/docker-compose.yml.backup.20260503165245
- lib/adapters/supabase/product-sync-repository.ts
- lib/services/product-search-service.ts
- lib/services/shopify-product-sync-service.ts
- n8n/workflows/youlya-whatsapp-main.json
- public/build-info.json
- RELEASES/v2.6.5-allow-n8n-workflow-env-access-for-youlya-workflow.md
- worktime.md
- PROGRESS-LOG.md

## Tests Required
- npm run typecheck
- npm run lint
- npm test
- npm run validate:scenarios
- npm run scan:secrets
- npm run build

## Deployment Notes
- Must pass verify before deploy.
- Must show version in dashboard/login footer.

## Status
PASS
