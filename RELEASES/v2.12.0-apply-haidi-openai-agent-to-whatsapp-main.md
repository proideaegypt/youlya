# Release: v2.12.0 — apply-haidi-openai-agent-to-whatsapp-main

## Task
apply-haidi-openai-agent-to-whatsapp-main

## Release Type
minor

## What Changed
- Replaced rule-based `Haidi AI Sales Agent` Code node in active n8n workflow `Youlya WhatsApp Main` (joqfame4HXG775JO) with a real OpenAI HTTP Request integration.
- Added `Build Haidi Prompt` Code node to construct system + user messages from app safety-gate output.
- Added `Call OpenAI` HTTP Request node (POST to `https://api.openai.com/v1/chat/completions`) using `gpt-4o-mini` with JSON response format.
- Added `Parse Haidi Response` Code node to extract JSON content from OpenAI `choices[0].message.content`.
- Updated `Validate Haidi Output` node to handle OpenAI response wrapper as a safety fallback.
- Repositioned downstream nodes (`Validate Haidi Output`, `Prepare Reply`, `Should Send Reply`, `Prepare Evolution Payload`, `Send Text`) for clean UI layout.
- Verified synthetic webhook tests return 200 and execution trace shows all Haidi nodes (`Build Haidi Prompt`, `Call OpenAI`, `Parse Haidi Response`, `Validate Haidi Output`) succeed.
- `Send Text` fails only on synthetic test numbers because Evolution API returns `exists: false` for fake WhatsApp JIDs — this is expected and confirms the Haidi layer itself is healthy.
- Fixed stale TypeScript cache (`tsconfig.tsbuildinfo`) causing false-positive `HandoffReason` type error.

## Files Changed
- ?? n8n/backups/workflow-joqfame4HXG775JO-pre-haidi.json
- ?? n8n/backups/workflow-joqfame4HXG775JO-haidi-openai.json
- M RELEASES/v2.12.0-apply-haidi-openai-agent-to-whatsapp-main.md
- M worktime.md

## Tests Required
- npm run typecheck
- npm run lint
- npm test
- npm run validate:scenarios
- npm run scan:secrets
- npm run build

## Deployment Notes
- Must pass verify before deploy.
- Must show version in dashboard/login footer.

## Status
PASS
