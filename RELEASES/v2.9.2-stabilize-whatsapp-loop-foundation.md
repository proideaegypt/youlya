# Release: v2.9.2 — stabilize-whatsapp-loop-foundation

## Task
stabilize-whatsapp-loop-foundation

## Release Type
patch

## What Changed
- Backed up the live `Youlya WhatsApp Main` workflow export before making any changes.
- Synced the canonical n8n workflow JSON to the live shape so `Send Text` uses a `Prepare Evolution Payload` code node plus `JSON.stringify(...)` instead of raw interpolated JSON.
- Confirmed the public webhook `https://ai.youlya365.com/webhook/youlya-whatsapp` returns `200` and starts the active workflow.
- Verified the live n8n container is using `EVOLUTION_INSTANCE=AI` and that Evolution reports that instance as `open`.
- Ran a raw synthetic inbound WhatsApp payload with `text: هاي`, which reached `Call Turn Endpoint`, `Prepare Reply`, `Prepare Evolution Payload`, and `Send Text`; the only outbound failure was the expected dummy-number `400 Bad Request` from Evolution.
- Logged the task in `worktime.md`.

## Files Changed
- `n8n/workflows/youlya-whatsapp-main.json`
- `qa-artifacts/tasks/2026-05-04/stabilize-whatsapp-loop-foundation/Youlya WhatsApp Main.json`
- `worktime.md`
- `PROGRESS-LOG.md`
- `README.md`
- `package.json`
- `RELEASES/v2.9.2-stabilize-whatsapp-loop-foundation.md`

## Tests Required
- npm run typecheck
- npm run lint
- npm test
- npm run validate:scenarios
- npm run scan:secrets
- npm run build

## Deployment Notes
- Must pass verify before deploy.
- Must show version in dashboard/login footer.

## Status
PARTIAL
