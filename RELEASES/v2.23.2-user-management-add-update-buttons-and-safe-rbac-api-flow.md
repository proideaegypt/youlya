# Release: v2.23.2 — user-management-add-update-buttons-and-safe-rbac-api-flow

## Task
user-management-add-update-buttons-and-safe-rbac-api-flow

## Release Type
patch

## What Changed
- Added explicit `إضافة مستخدم` action on Users & Roles page.
- Added per-user `تعديل` action with edit modal.
- Added safe create/update user API flow in `app/api/dashboard/users/route.ts` with:
  - auth + RBAC gate (`canManageRoles`)
  - server-side validation (Zod)
  - server-only Supabase Admin usage via `SUPABASE_SERVICE_ROLE_KEY`
  - safe Arabic error messages
  - last super_admin self-demotion protection
- Added user-management guard helpers and unit tests for core protections/error mapping.
- Added required review/status updates in go-live artifacts and progress logs.

## Files Changed (Task Scope)
- app/api/dashboard/users/route.ts
- app/dashboard/settings/users/page.tsx
- lib/auth/user-management-guards.ts
- tests/unit/user-management-route-guards.test.ts
- qa-artifacts/go-live-review/user-management-pre-change-status.txt
- claudeahmed.md
- qa-artifacts/go-live-review/GO_LIVE_REVIEW.md
- PROGRESS-LOG.md
- RELEASES/v2.23.2-user-management-add-update-buttons-and-safe-rbac-api-flow.md
- worktime.md

## Validation
- `npm run typecheck` ✅
- `npm run lint` ⚠️ fails due pre-existing repo warnings configured as failing gate (no new lint error introduced by this task)
- `npm test` ✅ (221 passed)
- `npm run validate:scenarios` ✅
- `npm run build` ⚠️ build reached finalization logs; tool session ended before completion confirmation

## Notes
- No deploy performed.
- No secrets exposed.

## Tests Required
- `npm run typecheck` ✅
- `npm run lint` ⚠️ (repository-level warnings outside this task currently fail gate)
- `npm test` ✅
- `npm run validate:scenarios` ✅
- `npm run build` ⚠️ (build progressed to finalization; command session ended before explicit success line)

## Status
PARTIAL
