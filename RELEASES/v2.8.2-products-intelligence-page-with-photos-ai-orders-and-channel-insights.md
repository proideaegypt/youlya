# Release: v2.8.2 — products-intelligence-page-with-photos-ai-orders-and-channel-insights

## Task
products-intelligence-page-with-photos-ai-orders-and-channel-insights

## Release Type
patch

## What Changed
- Extracted pure business logic from products-intelligence API routes into `lib/services/products-intelligence-service.ts`
- Added `normalizeChannel()` for safe channel enum normalization (whatsapp, instagram, tiktok, facebook, manual, unknown)
- Added `generateProductNotes()` for deterministic product insights from variant metrics
- Added `generateProductBadges()` for AI readiness, missing SKU, OOS, low stock, hidden badges
- Added `calculateIntelligenceScore()` weighted scoring (AI visibility 40%, SKU completeness 30%, stock 30%)
- Added `computeVariantAggregates()` for safe variant metric rollup with null handling
- Added `generateAiVisibilityReasons()` per-variant diagnostic reasons
- Added 33 unit tests in `tests/unit/products-intelligence-service.test.ts`
- Refactored all 4 API routes to use shared service functions:
  - `GET /api/dashboard/products-intelligence/overview`
  - `GET /api/dashboard/products-intelligence/products`
  - `GET /api/dashboard/products-intelligence/channels`
  - `GET /api/dashboard/products-intelligence/product/[id]`
- Added forward-only safe migration `20260504040000_product_notes.sql` for optional manual notes table
- Verified dashboard page `/dashboard/products-intelligence` renders with all required sections
- Verified sidebar menu item "ذكاء المنتجات / Products Intelligence" exists with Brain icon
- APIs require session cookie auth; no secrets exposed; no PII in responses
- Empty states shown for AI orders and channel analytics when data unavailable

## Files Changed
- `lib/services/products-intelligence-service.ts` (new)
- `tests/unit/products-intelligence-service.test.ts` (new)
- `supabase/migrations/20260504040000_product_notes.sql` (new)
- `app/api/dashboard/products-intelligence/overview/route.ts` (refactored)
- `app/api/dashboard/products-intelligence/products/route.ts` (refactored)
- `app/api/dashboard/products-intelligence/channels/route.ts` (refactored)
- `app/api/dashboard/products-intelligence/product/[id]/route.ts` (refactored)
- `package.json` (version bump)
- `worktime.md`, `PROGRESS-LOG.md`, `RELEASES/` (governance)

## Tests Required
- npm run typecheck (PASS)
- npm run lint (PASS, 0 errors)
- npm test (PASS, 91/91)
- npm run validate:scenarios (PASS, 104)
- npm run scan:secrets (PASS)
- npm run build (PASS)
- npm run verify:release (PASS)
- docker compose build (PASS)
- npm run deploy:production (PASS)
- live /api/health + /api/build-info (PASS)

## Deployment Notes
- Deployed to production VPS Docker via pull-based deploy agent.
- Live app running commit c734ecc.
- All dashboard products-intelligence APIs auth-gated and read-only from Shopify.

## Status
COMPLETED
