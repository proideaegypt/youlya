# Release: v2.20.2 — upgrade-haidi-app-prompt-to-v22-and-align-n8n-json-mode

## Task
upgrade-haidi-app-prompt-to-v22-and-align-n8n-json-mode

## Release Type
patch

## What Changed
- Upgraded `docs/HAIDI_AI_SALES_AGENT_PROMPT.md` to Haidi App Prompt v2.2:
  - Explicit app ownership of products/stock/prices/cart/orders/handoff
  - Haidi owns style/recommendations only from app facts
  - Short Egyptian Arabic replies
  - No fake stock/price/discount
  - No order confirmation unless app_action=order_created
  - Handoff only when app_action=handoff
  - JSON-only output schema with "Return valid JSON only."
- Aligned n8n backup workflow (active: joqfame4HXG775JO) with JSON mode requirements:
  - System prompt now includes "Return valid JSON only."
  - User content now includes "Return JSON only. Input JSON: ..."
  - `response_format: { type: 'json_object' }` preserved in Call OpenAI node
- Added handoff bypass in n8n workflow:
  - New "Check Handoff Bypass" IF node after Haidi Session Memory
  - New "Bypass Haidi" Code node for handoff/error paths
  - When action=handoff or action=error: skips OpenAI, uses app reply directly
  - Normal flow continues to Build Haidi Prompt → Call OpenAI → Parse → Validate
- Updated repo workflow `youlya-whatsapp-main.json`:
  - Haidi Code node now has explicit handoff bypass at the top
  - Uses v2.2 prompt principles
- Updated `lib/services/haidi-output-validator.ts`:
  - Blocks Haidi from overriding app handoff action (intent_label must be "handoff")
  - Blocks Haidi from overriding app order_created action (intent_label must be "confirm_order")
- Added comprehensive tests in `tests/unit/haidi-v22-compliance.test.ts`:
  - Prompt file contains "Return valid JSON only"
  - Backup workflow has response_format json_object and JSON in messages
  - Handoff bypass nodes exist and are wired correctly
  - Validator blocks action overrides
  - Plain text and empty JSON fallbacks work

## Files Changed
- M docs/HAIDI_AI_SALES_AGENT_PROMPT.md
- M lib/services/haidi-output-validator.ts
- M n8n/backups/workflow-joqfame4HXG775JO-haidi-openai.json
- M n8n/workflows/youlya-whatsapp-main.json
- A tests/unit/haidi-v22-compliance.test.ts
- A scripts/update-n8n-haidi-v22.mjs

## Tests Required
- npm run typecheck
- npm run lint
- npm test
- npm run validate:scenarios
- npm run scan:secrets
- npm run build
- npm run validate:n8n

## Deployment Notes
- n8n active workflow (joqfame4HXG775JO) must be updated via n8n UI or API import
- Backup workflow JSON is updated in repo but must be synced to n8n instance
- App Docker rebuild not strictly required (no app code changes), but recommended for consistency

## Status
PASS — All verification gates passed.
